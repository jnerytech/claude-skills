---
phase: 01-plugin-scaffolding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/improve-prompt/SKILL.md
  - skills/skill-create/SKILL.md
  - skills/skill-create/references/.gitkeep
  - skills/workspace-create/SKILL.md
  - skills/workspace-create/templates/.gitkeep
autonomous: true
requirements:
  - SETUP-02

must_haves:
  truths:
    - "skills/improve-prompt/, skills/skill-create/, and skills/workspace-create/ directories each exist"
    - "Each skill directory contains a SKILL.md with valid YAML frontmatter"
    - "SKILL.md frontmatter in each stub has the correct name, description, disable-model-invocation, and per-skill optional fields"
    - "skills/skill-create/references/ directory exists (via .gitkeep)"
    - "skills/workspace-create/templates/ directory exists (via .gitkeep)"
    - "Phase 2+ can edit stubs rather than create from scratch"
  artifacts:
    - path: "skills/improve-prompt/SKILL.md"
      provides: "Stub entrypoint for /improve-prompt skill"
      contains: "name: improve-prompt"
    - path: "skills/skill-create/SKILL.md"
      provides: "Stub entrypoint for /skill-create skill"
      contains: "name: skill-create"
    - path: "skills/workspace-create/SKILL.md"
      provides: "Stub entrypoint for /workspace-create skill"
      contains: "name: workspace-create"
    - path: "skills/skill-create/references/.gitkeep"
      provides: "Tracked placeholder for references/ directory"
    - path: "skills/workspace-create/templates/.gitkeep"
      provides: "Tracked placeholder for templates/ directory"
  key_links:
    - from: "skills/*/SKILL.md"
      to: "Claude Code plugin system"
      via: "skills/ directory convention + plugin.json"
      pattern: "name:"
---

<objective>
Create the three skill stub directories with valid SKILL.md placeholders and the supporting directories Phase 3 and 4 will populate.

Purpose: Phase 2+ work requires these files to exist with valid frontmatter so skill development can edit rather than create. The `references/` and `templates/` directories are pre-created so Phase 3 and 4 can start populating them without directory scaffolding work.
Output: Three `SKILL.md` stubs + two `.gitkeep` placeholder files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-plugin-scaffolding/01-CONTEXT.md
@.planning/phases/01-plugin-scaffolding/01-PATTERNS.md

<interfaces>
<!-- Canonical SKILL.md frontmatter — from installed plugin corpus -->
<!-- Source: C:/Users/dev/.claude/plugins/marketplaces/claude-plugins-official/plugins/example-plugin/skills/example-command/SKILL.md -->

Verified minimal frontmatter for a user-invoked skill:
```yaml
---
name: example-command
description: An example user-invoked skill that demonstrates frontmatter options and the skills/<name>/SKILL.md layout
argument-hint: <required-arg> [optional-arg]
allowed-tools: [Read, Glob, Grep, Bash]
---
```

D-05 (locked): Format is YAML frontmatter + Markdown body — NOT XML tags.
D-06 (locked): Path references inside SKILL.md bodies use `$USERPROFILE`, not `~` on Windows.

<!-- Per-skill frontmatter from PATTERNS.md (verified against research) -->

improve-prompt frontmatter:
```yaml
---
name: improve-prompt
description: "Rewrites a rough prompt for clarity, specificity, context richness, and structure. Use when the user invokes /improve-prompt or asks to 'improve this prompt', 'rewrite this prompt', or 'make this prompt clearer'. Do NOT use for general writing improvements unrelated to Claude Code prompts."
argument-hint: <rough-prompt-text>
disable-model-invocation: true
---
```

skill-create frontmatter:
```yaml
---
name: skill-create
description: "Interviews the user and generates a new Claude Code skill (SKILL.md), then writes it to ~/.claude/skills/<name>/SKILL.md. Use when the user invokes /skill-create or asks to 'create a skill', 'make a new skill', or 'build a slash command'. Do NOT use for general task automation unrelated to skill authoring."
argument-hint: [skill-description]
allowed-tools: [Read, Glob, Grep, Write, Bash]
disable-model-invocation: true
---
```

workspace-create frontmatter:
```yaml
---
name: workspace-create
description: "Guides a workspace setup interview and scaffolds .workspace/, .claude/, .vscode/ directories with a fully populated CLAUDE.md. Use when the user invokes /workspace-create or asks to 'set up a workspace', 'create a workspace', or 'scaffold a new workspace'. Do NOT use for setting up individual project repos."
allowed-tools: [Write, Bash]
disable-model-invocation: true
---
```

Note: `allowed-tools` format uses space-separated strings in some sources and array notation in others.
Use array notation `[Tool1, Tool2]` — confirmed from example-command SKILL.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create improve-prompt skill stub</name>
  <files>skills/improve-prompt/SKILL.md</files>
  <read_first>
    - C:/Users/dev/.claude/plugins/marketplaces/claude-plugins-official/plugins/example-plugin/skills/example-command/SKILL.md (analog — exact frontmatter shape)
    - .planning/phases/01-plugin-scaffolding/01-PATTERNS.md (improve-prompt section with exact frontmatter)
    - .planning/phases/01-plugin-scaffolding/01-CONTEXT.md (D-05 skill format, Claude's discretion on placeholder depth)
  </read_first>
  <action>
Create directory `skills/improve-prompt/` and file `skills/improve-prompt/SKILL.md`.

Write this exact content (frontmatter is final per PATTERNS.md; body is a Phase 2 placeholder stub as permitted by Claude's Discretion in CONTEXT.md):

```markdown
---
name: improve-prompt
description: "Rewrites a rough prompt for clarity, specificity, context richness, and structure. Use when the user invokes /improve-prompt or asks to 'improve this prompt', 'rewrite this prompt', or 'make this prompt clearer'. Do NOT use for general writing improvements unrelated to Claude Code prompts."
argument-hint: <rough-prompt-text>
disable-model-invocation: true
---

# Improve Prompt

The user invoked this with: $ARGUMENTS

<!-- Phase 2 will fill in the full rewrite instructions here. -->
<!-- Constraints to honor:
  - Output improved prompt + "what changed" annotation
  - Apply 4 dimensions: clarity/specificity, context richness, structure, scope/verification criteria
  - Inject Claude Code-specific idioms (@file references, verification asks, scope bounds)
  - Show original and improved prompt side by side
  - AskUserQuestion: max 4 options per call
-->
```

The frontmatter fields are:
- `name: improve-prompt` — exact slash command name; must match directory name
- `description` — third-person with quoted trigger phrases and "Do NOT use for" clause (required per PITFALLS Pitfall 4)
- `argument-hint: <rough-prompt-text>` — shown in autocomplete; required for this skill (takes user's rough prompt)
- `disable-model-invocation: true` — user controls invocation; prevents unintended auto-trigger (per STACK.md, CONTEXT D-05)

Do NOT add `allowed-tools` to this stub — improve-prompt makes no file I/O calls (confirmed in PATTERNS.md and FEATURES.md).
  </action>
  <verify>
    <automated>grep -q 'name: improve-prompt' skills/improve-prompt/SKILL.md && grep -q 'disable-model-invocation: true' skills/improve-prompt/SKILL.md && grep -q 'argument-hint' skills/improve-prompt/SKILL.md && grep -q '\$ARGUMENTS' skills/improve-prompt/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'name: improve-prompt' skills/improve-prompt/SKILL.md` exits 0
    - `grep -q 'disable-model-invocation: true' skills/improve-prompt/SKILL.md` exits 0
    - `grep -q 'argument-hint' skills/improve-prompt/SKILL.md` exits 0
    - `grep -q '\$ARGUMENTS' skills/improve-prompt/SKILL.md` exits 0
    - File does NOT contain `allowed-tools` (this skill needs no file I/O tools)
    - File contains a YAML frontmatter block (starts with `---` on line 1)
    - `grep -c '^---$' skills/improve-prompt/SKILL.md` outputs at least 2 (opening and closing `---` markers)
  </acceptance_criteria>
  <done>skills/improve-prompt/SKILL.md exists with valid YAML frontmatter (name, description, argument-hint, disable-model-invocation), $ARGUMENTS intake line, and Phase 2 placeholder body</done>
</task>

<task type="auto">
  <name>Task 2: Create skill-create skill stub and references/ directory</name>
  <files>skills/skill-create/SKILL.md, skills/skill-create/references/.gitkeep</files>
  <read_first>
    - C:/Users/dev/.claude/plugins/marketplaces/claude-plugins-official/plugins/skill-creator/skills/skill-creator/SKILL.md (secondary reference — interview flow patterns)
    - .planning/phases/01-plugin-scaffolding/01-PATTERNS.md (skill-create section — frontmatter and references/ pattern)
    - .planning/phases/01-plugin-scaffolding/01-CONTEXT.md (D-06 Windows path, AskUserQuestion 4-option constraint)
  </read_first>
  <action>
Create directory `skills/skill-create/` and file `skills/skill-create/SKILL.md`.
Also create `skills/skill-create/references/.gitkeep` (empty file to track the references/ directory in git).

Write this exact content for `skills/skill-create/SKILL.md`:

```markdown
---
name: skill-create
description: "Interviews the user and generates a new Claude Code skill (SKILL.md), then writes it to ~/.claude/skills/<name>/SKILL.md. Use when the user invokes /skill-create or asks to 'create a skill', 'make a new skill', or 'build a slash command'. Do NOT use for general task automation unrelated to skill authoring."
argument-hint: [skill-description]
allowed-tools: [Read, Glob, Grep, Write, Bash]
disable-model-invocation: true
---

# Skill Create

<!-- Phase 3 will fill in the full interview and generation instructions here. -->
<!-- Constraints to honor:
  - Read ${CLAUDE_SKILL_DIR}/docs/ before generating (docs/index.md first, then specific files)
  - Interview with 5-6 targeted questions; propose answers for user to react to
  - AskUserQuestion: max 4 options per call — design all question banks around this hard limit
  - Show generated SKILL.md for review before writing
  - Write to $USERPROFILE/.claude/skills/<name>/SKILL.md (Windows path — D-06)
  - Validate: skill name is kebab-case, no path traversal chars (/, .., \)
  - Confirm with user before writing
-->
```

The frontmatter fields are:
- `name: skill-create` — must match directory name exactly
- `description` — third-person with trigger phrases and "Do NOT use for" clause
- `argument-hint: [skill-description]` — square brackets indicate optional argument
- `allowed-tools: [Read, Glob, Grep, Write, Bash]` — pre-approved for docs read + global skill write
- `disable-model-invocation: true` — file writes must be user-initiated

For `skills/skill-create/references/.gitkeep`: create as an empty file. Git requires at least one file to track a directory. Phase 3 will populate this with `skill-anatomy.md`, `writing-guide.md`, `naming-conventions.md`.
  </action>
  <verify>
    <automated>grep -q 'name: skill-create' skills/skill-create/SKILL.md && grep -q 'disable-model-invocation: true' skills/skill-create/SKILL.md && grep -q 'allowed-tools' skills/skill-create/SKILL.md && test -f skills/skill-create/references/.gitkeep && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'name: skill-create' skills/skill-create/SKILL.md` exits 0
    - `grep -q 'disable-model-invocation: true' skills/skill-create/SKILL.md` exits 0
    - `grep -q 'allowed-tools' skills/skill-create/SKILL.md` exits 0
    - `grep -q 'Read' skills/skill-create/SKILL.md` exits 0 (Read is in allowed-tools)
    - `grep -q 'Write' skills/skill-create/SKILL.md` exits 0 (Write is in allowed-tools)
    - `grep -q 'Bash' skills/skill-create/SKILL.md` exits 0 (Bash is in allowed-tools)
    - `test -f skills/skill-create/references/.gitkeep` exits 0
    - File contains a YAML frontmatter block (starts with `---` on line 1)
    - Body comment mentions `AskUserQuestion` 4-option limit (Phase 3 constraint reminder)
  </acceptance_criteria>
  <done>skills/skill-create/SKILL.md exists with valid frontmatter including allowed-tools and disable-model-invocation; skills/skill-create/references/.gitkeep exists to track the directory</done>
</task>

<task type="auto">
  <name>Task 3: Create workspace-create skill stub and templates/ directory</name>
  <files>skills/workspace-create/SKILL.md, skills/workspace-create/templates/.gitkeep</files>
  <read_first>
    - .planning/phases/01-plugin-scaffolding/01-PATTERNS.md (workspace-create and templates/ sections)
    - .planning/phases/01-plugin-scaffolding/01-CONTEXT.md (D-06 Windows path, CONTEXT code_context: templates/ must exist before Phase 4)
    - .planning/FEATURES.md (workspace-create table stakes for body comment reference)
  </read_first>
  <action>
Create directory `skills/workspace-create/` and file `skills/workspace-create/SKILL.md`.
Also create `skills/workspace-create/templates/.gitkeep` (empty file to track the templates/ directory in git).

Write this exact content for `skills/workspace-create/SKILL.md`:

```markdown
---
name: workspace-create
description: "Guides a workspace setup interview and scaffolds .workspace/, .claude/, .vscode/ directories with a fully populated CLAUDE.md. Use when the user invokes /workspace-create or asks to 'set up a workspace', 'create a workspace', or 'scaffold a new workspace'. Do NOT use for setting up individual project repos."
allowed-tools: [Write, Bash]
disable-model-invocation: true
---

# Workspace Create

<!-- Phase 4 will fill in the full interview and scaffolding instructions here. -->
<!-- Constraints to honor:
  - AskUserQuestion: max 4 options per call — open-ended text for repo names and purpose
  - Interview captures: workspace name, repos list, per-repo purpose, overall workspace goal, conventions
  - Scaffold: .workspace/refs/, docs/, logs/, scratch/, context/, outputs/, sessions/
  - Also create: .claude/ and .vscode/ at workspace root
  - Write README.md in each .workspace/ subdir explaining its purpose
  - Load ${CLAUDE_SKILL_DIR}/templates/CLAUDE.md.template and populate from interview answers
  - CLAUDE.md must have zero stubs — use actual interview answers in every section
  - Validate workspace name: no spaces or special chars (becomes a directory name)
  - Confirm full scaffold with user before writing
  - All paths use forward slashes; do NOT hardcode user home paths
  - CLAUDE.md must be under 200 lines
-->
```

The frontmatter fields are:
- `name: workspace-create` — must match directory name exactly
- `description` — third-person with trigger phrases and "Do NOT use for" clause
- `allowed-tools: [Write, Bash]` — only needs Write (create files) and Bash (mkdir -p)
- `disable-model-invocation: true` — workspace creation is a major operation; user must initiate
- No `argument-hint` — this skill takes no arguments; omitting avoids a misleading empty hint

For `skills/workspace-create/templates/.gitkeep`: create as an empty file. Phase 4 will add `CLAUDE.md.template` and `dir-manifest.md` here.
  </action>
  <verify>
    <automated>grep -q 'name: workspace-create' skills/workspace-create/SKILL.md && grep -q 'disable-model-invocation: true' skills/workspace-create/SKILL.md && grep -q 'allowed-tools' skills/workspace-create/SKILL.md && test -f skills/workspace-create/templates/.gitkeep && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'name: workspace-create' skills/workspace-create/SKILL.md` exits 0
    - `grep -q 'disable-model-invocation: true' skills/workspace-create/SKILL.md` exits 0
    - `grep -q 'allowed-tools' skills/workspace-create/SKILL.md` exits 0
    - `grep -q 'Write' skills/workspace-create/SKILL.md` exits 0 (Write in allowed-tools)
    - `grep -q 'Bash' skills/workspace-create/SKILL.md` exits 0 (Bash in allowed-tools)
    - File does NOT contain `argument-hint` (this skill takes no arguments — omitting is correct)
    - `test -f skills/workspace-create/templates/.gitkeep` exits 0
    - File contains a YAML frontmatter block (starts with `---` on line 1)
    - Body comment mentions CLAUDE.md must be under 200 lines (project constraint from CLAUDE.md)
  </acceptance_criteria>
  <done>skills/workspace-create/SKILL.md exists with valid frontmatter (name, description, allowed-tools, disable-model-invocation, no argument-hint); skills/workspace-create/templates/.gitkeep exists to track the directory</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md body → Claude execution | Skill body content becomes instructions Claude follows at invocation time |
| $ARGUMENTS → SKILL.md body | User-supplied argument text is substituted into SKILL.md at invocation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | SKILL.md frontmatter `disable-model-invocation` field | accept | Phase 1 stubs have no executable body. Threat is deferred to Phases 2-4 where the body is implemented. Flag for those phases: `disable-model-invocation: true` must remain in all three stubs. |
| T-02-02 | Information Disclosure | `$ARGUMENTS` injection in improve-prompt | accept | Phase 1 stub body contains no processing logic — only a placeholder comment. Actual $ARGUMENTS handling is a Phase 2 concern. Note in stub body that $ARGUMENTS content must be treated as bounded user data, not trusted instructions. |
| T-02-03 | Elevation of Privilege | `allowed-tools` granting Bash and Write to skill-create | accept | Phase 1 stub has no executable instructions that invoke these tools. Phase 3 must restrict Write to `$USERPROFILE/.claude/skills/<sanitized-name>/SKILL.md` only, and validate skill name input before constructing the path. Documented in stub body comment. |
| T-02-04 | Elevation of Privilege | `allowed-tools: [Write, Bash]` on workspace-create | accept | Phase 1 stub has no executable instructions. Phase 4 must validate workspace name input and restrict writes to the cwd `.workspace/` tree. Documented in stub body comment. |
</threat_model>

<verification>
1. Confirm all three SKILL.md files exist and have valid frontmatter delimiters:
   `for f in skills/improve-prompt/SKILL.md skills/skill-create/SKILL.md skills/workspace-create/SKILL.md; do grep -c '^---$' "$f"; done`
   Each should output at least 2.

2. Confirm no allowed-tools on improve-prompt (it needs none):
   `grep -L 'allowed-tools' skills/improve-prompt/SKILL.md`
   Should return the filename (no match found).

3. Confirm gitkeep placeholders exist:
   `ls skills/skill-create/references/.gitkeep skills/workspace-create/templates/.gitkeep`
</verification>

<success_criteria>
- `skills/improve-prompt/SKILL.md` has name, description, argument-hint, disable-model-invocation=true, $ARGUMENTS intake, no allowed-tools
- `skills/skill-create/SKILL.md` has name, description, argument-hint, allowed-tools=[Read,Glob,Grep,Write,Bash], disable-model-invocation=true
- `skills/workspace-create/SKILL.md` has name, description, allowed-tools=[Write,Bash], disable-model-invocation=true, no argument-hint
- `skills/skill-create/references/.gitkeep` exists
- `skills/workspace-create/templates/.gitkeep` exists
- All three SKILL.md descriptions are in third-person with quoted trigger phrases and "Do NOT use for" clauses
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-scaffolding/01-02-SUMMARY.md` using the summary template.
</output>
