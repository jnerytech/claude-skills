---
phase: 02-prompt-improvement-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/improve-prompt/SKILL.md
autonomous: true
requirements:
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03
  - PROMPT-04
  - PROMPT-05

must_haves:
  truths:
    - "Invoking `/improve-prompt fix the auth bug` produces chat output containing three `##` sections in order: `## Original`, `## Improved`, `## What Changed`"
    - "Both the Original and Improved sections render the prompt inside fenced code blocks (copyable)"
    - "Improved prompt for a code-change task contains an `@file` reference and a verification step"
    - "Each `## What Changed` bullet matches the pattern `- [Change label] — [one-sentence reason]`"
    - "Invoking `/improve-prompt` with no argument outputs a usage message and does not produce a rewrite"
    - "Invoking `/improve-prompt fix it` (low-info input) produces bracketed placeholders in the improved prompt and a `⚠️ To sharpen further, add:` note after `## What Changed`"
    - "No file writes occur during invocation — the skill has `disable-model-invocation: true` and no `allowed-tools`"
  artifacts:
    - path: "skills/improve-prompt/SKILL.md"
      provides: "Working instruction body that teaches Claude the four-dimension rewrite algorithm, the heuristic idiom-injection rules, and the locked three-section output format"
      contains: "## What Changed"
      min_lines: 80
  key_links:
    - from: "skills/improve-prompt/SKILL.md frontmatter (lines 1-6)"
      to: "Frozen — must not be modified by Phase 2"
      via: "Edit scope restricted to lines 8 onward"
      pattern: "disable-model-invocation: true"
    - from: "skills/improve-prompt/SKILL.md body — intake anchor"
      to: "$ARGUMENTS substitution"
      via: "Line `The user invoked this with: $ARGUMENTS` preserved at line 10"
      pattern: "The user invoked this with: \\$ARGUMENTS"
    - from: "Heuristic idiom-injection table"
      to: "D-05 cues (filename / code-change verb / unbounded scope)"
      via: "Markdown table with Idiom | Inject when | Example columns"
      pattern: "\\| Idiom \\|"
    - from: "Worked example block"
      to: "Canonical output from CONTEXT.md `<specifics>`"
      via: "4-backtick outer fence wrapping nested triple-backtick blocks"
      pattern: "^````"
---

<objective>
Replace the placeholder body in `skills/improve-prompt/SKILL.md` with a working instruction body that teaches Claude how to rewrite a rough prompt into a clearer, structured Claude-Code-native prompt and emit it in the locked three-section output format.

Purpose: This single file IS the deliverable for Phase 2. The frontmatter is already in place from Phase 1. Phase 2's job is to write the imperative instruction body that turns `$ARGUMENTS` into a high-quality rewrite plus a "what changed" annotation, with no file writes and no external calls.

Output: One modified file — `skills/improve-prompt/SKILL.md` — with a complete instruction body honoring all eight locked decisions (D-01 through D-08) and the empty-args guard (Pitfall 2 from RESEARCH.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-prompt-improvement-skill/02-CONTEXT.md
@.planning/phases/02-prompt-improvement-skill/02-RESEARCH.md
@.planning/phases/02-prompt-improvement-skill/02-PATTERNS.md
@.planning/phases/02-prompt-improvement-skill/02-VALIDATION.md

<interfaces>
<!-- Current state of the file being edited — frontmatter is FROZEN from Phase 1. -->
<!-- Source: skills/improve-prompt/SKILL.md (read it before editing). -->

Lines 1-6 (frozen — do NOT modify):
```yaml
---
name: improve-prompt
description: "Rewrites a rough prompt for clarity, specificity, context richness, and structure. Use when the user invokes /improve-prompt or asks to 'improve this prompt', 'rewrite this prompt', or 'make this prompt clearer'. Do NOT use for general writing improvements unrelated to Claude Code prompts."
argument-hint: <rough-prompt-text>
disable-model-invocation: true
---
```

Line 8 (frozen — keep as-is): `# Improve Prompt`

Line 10 (intake anchor — frozen — keep as-is): `The user invoked this with: $ARGUMENTS`

Lines 12-19 (placeholder comment block — REPLACE entirely with the body skeleton in Task 1's action).

<!-- Locked output format (D-01, D-02): three `##` sections, both prompts in fenced code blocks. -->
<!-- Locked annotation format (D-03, D-04): bullets `- [Label] — [reason]`, only for actual changes. -->
<!-- Locked idiom logic (D-05, D-06): heuristic, cue-based, infer @file path when filename implied. -->
<!-- Locked low-info rule (D-07, D-08): bracketed placeholders + optional sharpen note. -->
<!-- Recommended (Pitfall 2): empty $ARGUMENTS guard returns a usage message and stops. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the improve-prompt SKILL.md instruction body</name>
  <files>skills/improve-prompt/SKILL.md</files>
  <read_first>
    - skills/improve-prompt/SKILL.md (current state — frontmatter on lines 1-6 is FROZEN; line 8 `# Improve Prompt` and line 10 intake anchor are FROZEN; lines 12-19 placeholder is the replacement target)
    - .planning/phases/02-prompt-improvement-skill/02-CONTEXT.md (locked decisions D-01 through D-08, canonical output examples in `<specifics>`)
    - .planning/phases/02-prompt-improvement-skill/02-RESEARCH.md (Pitfalls 1-4, four-dimension concrete definitions in Pitfall 4, Code Examples section, Pattern 1/2/3)
    - .planning/phases/02-prompt-improvement-skill/02-PATTERNS.md (intake anchor rule, frontmatter freeze, sharpen-note rule, three-section output template)
    - .planning/phases/02-prompt-improvement-skill/02-VALIDATION.md (per-task verification map and manual UAT cases the body must satisfy)
  </read_first>
  <action>
**Edit scope:** Modify `skills/improve-prompt/SKILL.md` ONLY from line 12 onward. Lines 1-6 (YAML frontmatter), line 8 (`# Improve Prompt`), and line 10 (`The user invoked this with: $ARGUMENTS`) MUST remain byte-for-byte unchanged. Line 9 and line 11 are blank lines — preserve them. Delete lines 12-19 (the `<!-- Phase 2 will fill in...` comment block) and replace with the body below.

**The replacement body — write this content starting at line 12:**

````markdown
## When to act

If `$ARGUMENTS` is empty or contains only whitespace, output exactly:

> Provide the rough prompt you want improved as an argument, for example:
> `/improve-prompt fix the auth bug`

Then stop — do not attempt a rewrite, do not ask a clarifying question.

Otherwise, treat everything in `$ARGUMENTS` as the rough prompt the user wants rewritten, and proceed.

## How to rewrite

Apply these four dimensions to the rough prompt. Each definition is a concrete action verb — apply only the dimensions that need work, not all four mechanically:

- **Clarity / specificity** — Name the exact file, function, or behavior involved. Replace pronouns like "it" or "that" with concrete nouns.
- **Context richness** — Add an `@file` reference when a file is mentioned or implied. Add the relevant constraint (language, framework, version) when known.
- **Structure** — Open with an imperative verb. Separate "what to do" from "how to verify" into distinct sentences or a small numbered list.
- **Scope / verification** — State what Claude should NOT change. Specify how to confirm the task is done (a test command, a behavior to observe, an acceptance check).

When rewriting the Improved section, render the user's task in your own clearer words — but never lose the user's intent. The Original section, by contrast, is the user's input verbatim (see "How to format output" below).

## Inject Claude Code idioms — only on cue

Apply these idioms only when their cue is present in the rough prompt. Do not add them indiscriminately:

| Idiom | Inject when | Example |
|-------|-------------|---------|
| `@file <path>` | A filename, path, or module name is mentioned or clearly implied (e.g. "the auth middleware") | `@file src/auth/middleware.ts` |
| Verification ask | The task verb is fix, refactor, implement, migrate, add, update, or another code-change verb | "Run `npm test -- auth` to verify no regressions" |
| Scope bounds | The prompt has no clear success criteria, or could be interpreted too broadly | "Focus only on the token expiry check; do not refactor the surrounding handler" |

When a filename is implied but not stated (e.g. the user says "auth middleware"), infer a plausible `@file` path rather than omitting the reference. A concrete-but-approximate reference like `@file auth/middleware.ts` is more useful than no reference, because Claude at invocation time will correct wrong guesses.

## Handle low-information input

If the rough prompt is too vague to rewrite with full specificity (it is short, names no file, and describes no scope), produce the best rewrite you can using bracketed placeholders to mark structurally missing context. Examples of placeholders: `[describe symptom]`, `[specify the target file]`, `[expected vs actual behavior]`.

Do not ask a clarifying question first — the user invoked `/improve-prompt` for a fast turnaround. Always produce a rewrite.

When (and only when) the input was detectably low-information — short, no file references, no describable scope — append a sharpen note after `## What Changed`:

> ⚠️ To sharpen further, add: [comma-separated list of context items that would improve the rewrite]

Do not add this note to already-decent prompts. Signal-to-noise matters.

## How to format output

Output exactly three `##` sections in this order. Render both prompts inside triple-backtick fenced code blocks so the user can copy them. The `## What Changed` section is a bullet list, never a paragraph.

```
## Original
```
[the user's rough prompt — verbatim, unchanged, do not "lightly clean up" the wording]
```

## Improved
```
[the rewritten prompt]
```

## What Changed
- [Change label] — [one-sentence reason why it improves the prompt]
- [Change label] — [one-sentence reason]
```

Each bullet in `## What Changed` follows the pattern `- [Change label] — [one-sentence reason why it improves the prompt]`.

Only emit a bullet for a change you actually made. If a dimension was already strong in the original, omit its bullet — the goal is signal density, not exhaustive coverage. The "most impactful change first" ordering is preferred but not enforced.

If the input was low-information, append the `⚠️ To sharpen further, add:` note on its own line below the bullet list.

## Worked example — desired output for a code-change prompt

When the user runs `/improve-prompt fix the auth bug`, your output should look like this:

````markdown
## Original
```
fix the auth bug
```

## Improved
```
In auth/middleware.ts, the token expiry check uses `<` instead of `<=`. Fix it and run `npm test -- auth` to verify no regressions.
```

## What Changed
- Added @file reference — gives Claude a precise entry point instead of searching the whole codebase
- Added scope: narrowed to token expiry logic — prevents Claude from refactoring unrelated code
- Added verification step — gives Claude a way to confirm its fix worked without being asked
````

(The 4-backtick fence above is only how this example is encoded inside SKILL.md so the inner triple-backticks render correctly. Your actual chat output uses triple-backtick fences as shown in the "How to format output" section.)

## Worked example — desired output for a low-information prompt

When the user runs `/improve-prompt fix it`, your output should look like this:

````markdown
## Original
```
fix it
```

## Improved
```
Identify and fix the bug in [specify the target file] causing [describe symptom]. After fixing, run the relevant tests to confirm the regression is resolved.
```

## What Changed
- Added symptom placeholder — Claude needs to know what is broken to find the right bug
- Added verification step — gives Claude a way to confirm the fix worked

⚠️ To sharpen further, add: which file or feature is affected, and what the expected vs actual behavior is.
````

## Final checks before responding

Before sending output, confirm:
1. Three `##` sections are present in this exact order: Original, Improved, What Changed.
2. The Original section contains the user's input verbatim, inside a fenced code block.
3. The Improved section contains the rewrite, inside a fenced code block.
4. Every bullet under `## What Changed` matches `- [Label] — [reason]`.
5. Idioms (`@file`, verification, scope bounds) only appear if their cue was present.
6. The sharpen note appears only if the input was detectably low-information.
7. No file was written; no tool was invoked. The output is chat-only.
````

**End of replacement body.** After the closing line (the final `7. No file...` instruction), there is no further content — the file ends there.

**Concrete edit instructions:**

1. Read `skills/improve-prompt/SKILL.md` to confirm current state matches the interfaces block above.
2. Replace lines 12-19 (the `<!-- Phase 2 will fill in...` comment block, ending at the closing `-->`) with the body shown above (everything from `## When to act` through the final `7. No file was written...` line).
3. Do NOT modify lines 1-6 (frontmatter), line 8 (`# Improve Prompt`), or line 10 (`The user invoked this with: $ARGUMENTS`). Preserve blank lines 7, 9, and 11.
4. The body uses 4-backtick outer fences in the two worked-example blocks (per RESEARCH.md Pitfall 1) so that the inner triple-backtick fences render correctly when Claude reads the file. The "How to format output" section uses triple backticks because it shows Claude what to *output*, not a self-contained example block.
5. After saving, verify with `git diff skills/improve-prompt/SKILL.md` that no line at or above the second `---` was touched (frontmatter freeze per RESEARCH.md Pitfall 3).

**Why these specifics matter:**

- **Empty-args guard** — Pitfall 2 in RESEARCH.md flags this as a recommended addition (not locked in CONTEXT.md). Including it gives a defined zero-input behavior; VALIDATION.md already lists this as a manual UAT case.
- **Verbatim Original section** — D-01 and D-02 lock the layout; the canonical example in CONTEXT.md `<specifics>` shows the original input rendered as-is. The instruction "verbatim, unchanged, do not lightly clean up the wording" prevents Claude from preprocessing the input on rewrite, which would break PROMPT-03's side-by-side semantics.
- **4-backtick outer fences in worked examples** — Pitfall 1: triple-backtick inner fences would prematurely close a triple-backtick outer fence when Claude reads SKILL.md. Without this fix, the worked examples render as broken text and Claude may not learn the pattern correctly.
- **Heuristic table over prose** — Pattern 3 in RESEARCH.md and the api-conventions analog in PATTERNS.md confirm tables produce more deterministic conditional behavior than prose for multi-rule cases like idiom injection.
- **Imperative voice with brief reasoning** — Pattern 1 in RESEARCH.md and official Claude Code skill docs warn against narrative voice and ALWAYS/NEVER without rationale.
  </action>
  <verify>
    <automated>grep -q '^## Original' skills/improve-prompt/SKILL.md && grep -q '^## Improved' skills/improve-prompt/SKILL.md && grep -q '^## What Changed' skills/improve-prompt/SKILL.md && grep -q '^name: improve-prompt$' skills/improve-prompt/SKILL.md && grep -q '^disable-model-invocation: true$' skills/improve-prompt/SKILL.md && grep -q '^argument-hint: <rough-prompt-text>$' skills/improve-prompt/SKILL.md && grep -q 'The user invoked this with: \$ARGUMENTS' skills/improve-prompt/SKILL.md && grep -q '| Idiom |' skills/improve-prompt/SKILL.md && grep -q '^````' skills/improve-prompt/SKILL.md && grep -q 'To sharpen further, add' skills/improve-prompt/SKILL.md && grep -q 'empty or contains only whitespace' skills/improve-prompt/SKILL.md && ! grep -q 'Phase 2 will fill in' skills/improve-prompt/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^## Original' skills/improve-prompt/SKILL.md` exits 0 (Original section header present at start of a line)
    - `grep -q '^## Improved' skills/improve-prompt/SKILL.md` exits 0 (Improved section header present)
    - `grep -q '^## What Changed' skills/improve-prompt/SKILL.md` exits 0 (What Changed section header present)
    - `grep -q '^name: improve-prompt$' skills/improve-prompt/SKILL.md` exits 0 (frontmatter `name` line unchanged)
    - `grep -q '^disable-model-invocation: true$' skills/improve-prompt/SKILL.md` exits 0 (frontmatter `disable-model-invocation` line unchanged)
    - `grep -q '^argument-hint: <rough-prompt-text>$' skills/improve-prompt/SKILL.md` exits 0 (frontmatter `argument-hint` line unchanged)
    - `grep -q 'The user invoked this with: \$ARGUMENTS' skills/improve-prompt/SKILL.md` exits 0 (intake anchor preserved)
    - `grep -q '| Idiom |' skills/improve-prompt/SKILL.md` exits 0 (heuristic idiom-injection table present)
    - `grep -q '@file' skills/improve-prompt/SKILL.md` exits 0 (Claude Code idiom referenced)
    - `grep -q 'verification' skills/improve-prompt/SKILL.md` exits 0 (verification idiom mentioned)
    - `grep -qE '^\`\`\`\`' skills/improve-prompt/SKILL.md` exits 0 (4-backtick outer fence used in worked example)
    - `grep -q 'To sharpen further, add' skills/improve-prompt/SKILL.md` exits 0 (sharpen-note phrase from D-07 present)
    - `grep -q 'empty or contains only whitespace' skills/improve-prompt/SKILL.md` exits 0 (empty-args guard from Pitfall 2 present)
    - `grep -q 'Phase 2 will fill in' skills/improve-prompt/SKILL.md` exits NON-zero (placeholder comment removed)
    - `grep -q 'verbatim' skills/improve-prompt/SKILL.md` exits 0 (Original-as-verbatim instruction present)
    - `[ "$(wc -l < skills/improve-prompt/SKILL.md)" -ge 80 ]` is true (body has at least ~80 lines, indicating real content not a stub)
    - `git diff skills/improve-prompt/SKILL.md | grep -E '^[-+]' | head -8` shows no `+` or `-` lines that touch the frontmatter (lines at or above the second `---`)
  </acceptance_criteria>
  <done>`skills/improve-prompt/SKILL.md` has the placeholder comment block replaced with a working instruction body covering: empty-args guard, four-dimension rewrite definitions, heuristic idiom-injection table, low-info bracketed-placeholder rule with sharpen note, three-section output format, two worked examples using 4-backtick outer fences, and a final-checks list. Frontmatter (lines 1-6), `# Improve Prompt` heading (line 8), and intake anchor (line 10) are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → SKILL.md body → Claude chat output | User-supplied rough prompt enters as `$ARGUMENTS` and is rendered into chat output. No code execution, no file write, no network call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `skills/improve-prompt/SKILL.md` frontmatter | mitigate | Plan explicitly scopes edits to lines 12 onward; acceptance criteria grep-check `name:`, `disable-model-invocation: true`, and `argument-hint:` lines remain unchanged; `git diff` review confirms no frontmatter modification (RESEARCH.md Pitfall 3). |
| T-02-02 | Information Disclosure | User-supplied prompt content | accept | Skill has `disable-model-invocation: true` and no `allowed-tools`. Output stays in the user's own chat session; no file write, no network call, no persistence. User-supplied arguments are bounded data per REQUIREMENTS.md "Out of Scope: Prompt injection protection". |
| T-02-03 | Elevation of Privilege | Skill body instructions | accept | The body is read-only Markdown instructions Claude reasons over. There is no executable surface — no Bash, no Read, no Write tool calls in the rendered SKILL.md. ASVS V2/V3/V4/V5/V6 do not apply (RESEARCH.md Security Domain). |
</threat_model>

<verification>
After Task 1 completes, run the structural automated checks (per VALIDATION.md sampling rate):

1. **Structural grep checks** — run the Task 1 `<automated>` command; must print `PASS`.
2. **Frontmatter freeze** — `git diff skills/improve-prompt/SKILL.md` shows no `+`/`-` lines above the second `---` marker.
3. **Manual UAT in Claude Code** (per VALIDATION.md Manual-Only Verifications):
   - `/improve-prompt fix the auth bug` — output has three sections in order, contains `@file`, contains a verification step, bullets match `- [Label] — [reason]` format. (PROMPT-02, PROMPT-04, PROMPT-05)
   - `/improve-prompt explain how React hooks work` — no `@file` injection (cue absent — D-05 heuristic check). (PROMPT-05)
   - `/improve-prompt fix it` — output uses bracketed placeholders and includes the `⚠️ To sharpen further, add:` note. (PROMPT-01 D-07/D-08)
   - `/improve-prompt` (no argument) — output is the usage message, no rewrite attempted. (Pitfall 2 guard)
4. **No file writes during invocation** — confirm Claude Code session shows no Write or Bash tool calls during any of the manual UAT runs (PROMPT-01).
</verification>

<success_criteria>
- `skills/improve-prompt/SKILL.md` contains a working instruction body — no `<!-- Phase 2 will fill in...` placeholder remains.
- Three required `##` sections (`## Original`, `## Improved`, `## What Changed`) are documented in the body's output-format instructions.
- The body contains the heuristic idiom-injection table (D-05) with `@file`, verification, and scope bounds rows.
- The body defines all four rewrite dimensions concretely (per RESEARCH.md Pitfall 4): clarity/specificity, context richness, structure, scope/verification.
- The body includes the empty-`$ARGUMENTS` guard (per RESEARCH.md Pitfall 2).
- The body includes the low-info `⚠️ To sharpen further, add:` rule (D-07/D-08).
- The body contains at least one worked example using a 4-backtick outer fence (per RESEARCH.md Pitfall 1).
- Frontmatter (lines 1-6) is byte-for-byte unchanged from the Phase 1 stub (per RESEARCH.md Pitfall 3).
- Intake anchor on line 10 is preserved (per PATTERNS.md Shared Patterns).
- All five Phase 2 requirements (PROMPT-01 through PROMPT-05) are addressable by manual UAT against the canonical examples in CONTEXT.md `<specifics>`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-prompt-improvement-skill/02-01-SUMMARY.md` using the summary template. The summary should record:
- Final line count of the SKILL.md body
- Confirmation that frontmatter was untouched (`git diff` evidence)
- Manual UAT results for the three test prompts in `<verification>` step 3
- Any deviations from the planned body skeleton (and rationale)
</output>
