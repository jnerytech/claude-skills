---
phase: 04-workspace-creator-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/workspace-create/SKILL.md
  - skills/workspace-create/templates/CLAUDE.md.template
autonomous: true
requirements: [WORK-01, WORK-02, WORK-03, WORK-04, WORK-05, WORK-06]

must_haves:
  truths:
    - "User invokes /workspace-create and is guided through a chat interview capturing: workspace name, repos to include, per-repo purpose, overall workspace goal, and primary stack"
    - "User sees a scaffold tree preview before any file is written"
    - "After user confirms with 'yes', the workspace directory exists in CWD with full subdirectory structure: .workspace/refs/, docs/, logs/, scratch/, context/, outputs/, sessions/, .claude/, .vscode/"
    - "workspace/CLAUDE.md is populated from interview answers with zero {{MARKER}} patterns remaining in the file"
    - "wc -l on the generated CLAUDE.md returns < 200"
    - "Each .workspace/ subdirectory contains a one-line README.md explaining its purpose"
  artifacts:
    - path: "skills/workspace-create/SKILL.md"
      provides: "Full instruction body for the workspace-create skill"
      min_lines: 150
      contains: "Stage 1 through Stage 9 headers"
    - path: "skills/workspace-create/templates/CLAUDE.md.template"
      provides: "CLAUDE.md template with 6 {{MARKER}} placeholders"
      contains: "{{WORKSPACE_NAME}}"
  key_links:
    - from: "skills/workspace-create/SKILL.md (Stage 6)"
      to: "skills/workspace-create/templates/CLAUDE.md.template"
      via: "Read tool path: ${CLAUDE_SKILL_DIR}/templates/CLAUDE.md.template"
      pattern: "CLAUDE_SKILL_DIR.*templates.*CLAUDE\\.md\\.template"
    - from: "skills/workspace-create/SKILL.md (Stage 6 marker replacement)"
      to: "skills/workspace-create/templates/CLAUDE.md.template (markers)"
      via: "exact marker name matching: {{WORKSPACE_NAME}}, {{WORKSPACE_GOAL}}, {{REPO_MAP}}, {{STACK}}, {{CREATED_DATE}}, {{CONVENTIONS}}"
      pattern: "\\{\\{WORKSPACE_NAME\\}\\}"
    - from: "skills/workspace-create/SKILL.md (Stage 4 scaffold)"
      to: "workspace directory on disk"
      via: "Bash mkdir -p using WORKSPACE_ROOT=$(pwd)/<validated-name>"
      pattern: "\\$\\(pwd\\)"
---

<objective>
Write the instruction body of skills/workspace-create/SKILL.md and create the CLAUDE.md template file that the skill loads at runtime.

Purpose: This is the final skill of the plugin. A user runs /workspace-create, answers 5 chat interview questions (name, repos, per-repo purpose, goal, stack), sees a scaffold plan preview, confirms, and receives a fully-structured workspace directory in CWD with a populated CLAUDE.md. Zero placeholder markers may reach the final CLAUDE.md file.

Output:
- skills/workspace-create/SKILL.md — instruction body replacing the placeholder comment block (frontmatter amended to add Read to allowed-tools per Deviation D-01)
- skills/workspace-create/templates/CLAUDE.md.template — new file with 6 {{MARKERS}} the skill will populate at runtime
</objective>

<execution_context>
@D:/repos/claude-skills/.planning/phases/04-workspace-creator-skill/../../phases/04-workspace-creator-skill/../../../.claude/get-shit-done/workflows/execute-plan.md
@D:/repos/claude-skills/.planning/phases/04-workspace-creator-skill/../../phases/04-workspace-creator-skill/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/repos/claude-skills/.planning/PROJECT.md
@D:/repos/claude-skills/.planning/ROADMAP.md
@D:/repos/claude-skills/.planning/STATE.md

<!-- Phase research and pattern map — read in full for Task 2 -->
@D:/repos/claude-skills/.planning/phases/04-workspace-creator-skill/04-RESEARCH.md
@D:/repos/claude-skills/.planning/phases/04-workspace-creator-skill/04-PATTERNS.md
@D:/repos/claude-skills/.planning/phases/04-workspace-creator-skill/04-VALIDATION.md

<interfaces>
<!-- Current workspace-create stub (lines 1-23) -->
From skills/workspace-create/SKILL.md:
```yaml
---
name: workspace-create
description: "Guides a workspace setup interview..."
allowed-tools: [Write, Bash]
disable-model-invocation: true
---
```
<!-- Comment block on lines 10-23 lists the constraints to honor -->

<!-- Analog skill structure — exact stage order to mirror -->
From skills/skill-create/SKILL.md (stage sequence):
  Stage 1: ## When to act (guard on $ARGUMENTS)
  Stage 2: ## Read reference documentation
  Stage 3: ## Infer and confirm the skill name
  Stage 4: ## Adaptive interview (AskUserQuestion)
  Stage 5: ## Conditional reference reads
  Stage 6: ## Generate the SKILL.md content
  Stage 7: ## Preview the generated skill
  Stage 8: ## Write the skill (validate → mkdir → Write)
  Stage 9: ## Confirm
  Section:  ## Worked example
  Section:  ## Final checks before writing

<!-- Template markers the SKILL.md must reference by exact name -->
Markers defined in CLAUDE.md.template (Task 1 creates these):
  {{WORKSPACE_NAME}}  — from interview Q1 (required; re-ask if blank)
  {{WORKSPACE_GOAL}}  — from interview Q4 (required; re-ask if blank)
  {{REPO_MAP}}        — table rows "| repo | purpose |" from Q2+Q3 (fallback: "No repos specified")
  {{STACK}}           — from interview Q5 (fallback: "Not specified")
  {{CREATED_DATE}}    — from Bash `date +%Y-%m-%d` (fallback: static date if Bash fails)
  {{CONVENTIONS}}     — derived from stack + goal (fallback: "Follow standard conventions for the stack")
</interfaces>
</context>

## Deviations

**D-01 (Phase 4): Frontmatter `allowed-tools` amended to add `Read`.**
- Stub frontmatter: `allowed-tools: [Write, Bash]`
- Required: `allowed-tools: [Read, Write, Bash]`
- Reason: Stage 6 of the skill must Read `${CLAUDE_SKILL_DIR}/templates/CLAUDE.md.template` to perform marker substitution. The stub's own comment on line 17 states "Load ... CLAUDE.md.template" — Read was always the design intent. Approach A (separate template file) is chosen over Approach B (inline generation) for maintainability.
- Documented here per RESEARCH.md "Critical Open Design Questions Q2".

<tasks>

<task type="auto">
  <name>Task 1: Create CLAUDE.md.template with all 6 markers</name>

  <read_first>
    - skills/workspace-create/templates/.gitkeep (current state — confirms only .gitkeep exists; template does not yet exist)
    - .planning/phases/04-workspace-creator-skill/04-PATTERNS.md lines 203-264 (template skeleton and exact marker list)
    - .planning/phases/04-workspace-creator-skill/04-RESEARCH.md lines 299-332 (CLAUDE.md Template Design — section headings, line budget, marker fallbacks)
    - CLAUDE.md (project) — structural reference for a real workspace-style CLAUDE.md under 200 lines
  </read_first>

  <files>skills/workspace-create/templates/CLAUDE.md.template</files>

  <action>
Create `skills/workspace-create/templates/CLAUDE.md.template` as a new file. Delete the `.gitkeep` file in that directory (use Bash: `rm skills/workspace-create/templates/.gitkeep`).

The template content must follow this exact skeleton (from PATTERNS.md lines 213-251). Write each section verbatim — this is the authoritative marker set that Task 2's SKILL.md body must reference by exact name:

```
# {{WORKSPACE_NAME}}

{{WORKSPACE_GOAL}}

**Created:** {{CREATED_DATE}}

## Repositories

| Repo | Purpose |
|------|---------|
{{REPO_MAP}}

## Directory Map

| Directory | Purpose |
|-----------|---------|
| `.workspace/refs/` | Reference materials and external resources |
| `.workspace/docs/` | Documentation files and guides |
| `.workspace/logs/` | Session logs and activity records |
| `.workspace/scratch/` | Temporary scratch space for experiments |
| `.workspace/context/` | Persistent context files shared across sessions |
| `.workspace/outputs/` | Generated outputs and artifacts |
| `.workspace/sessions/` | Per-session working directories |

## Stack / Language

{{STACK}}

## Session Conventions

- Start each session by reviewing `.workspace/context/` for persistent notes
- Log session summaries to `.workspace/logs/`
- Place experimental work in `.workspace/scratch/` before promoting to a repo

## Constraints

{{CONVENTIONS}}
```

Rules:
- All 6 markers must appear exactly as shown: `{{WORKSPACE_NAME}}`, `{{WORKSPACE_GOAL}}`, `{{CREATED_DATE}}`, `{{REPO_MAP}}`, `{{STACK}}`, `{{CONVENTIONS}}`
- Do not add any prose above the `# {{WORKSPACE_NAME}}` line — the heading IS the first line
- The Directory Map section is static (not a marker) — hardcode the 7 subdirectory rows
- The Session Conventions section is static — do not make it a marker
- Target: ~50-55 lines. Do not add padding. Line budget is well within 200-line limit.
  </action>

  <verify>
    <automated>
      test -f skills/workspace-create/templates/CLAUDE.md.template && echo "EXISTS" || echo "MISSING"
      grep -c '{{WORKSPACE_NAME}}' skills/workspace-create/templates/CLAUDE.md.template
      grep -c '{{WORKSPACE_GOAL}}' skills/workspace-create/templates/CLAUDE.md.template
      grep -c '{{REPO_MAP}}' skills/workspace-create/templates/CLAUDE.md.template
      grep -c '{{STACK}}' skills/workspace-create/templates/CLAUDE.md.template
      grep -c '{{CREATED_DATE}}' skills/workspace-create/templates/CLAUDE.md.template
      grep -c '{{CONVENTIONS}}' skills/workspace-create/templates/CLAUDE.md.template
      wc -l skills/workspace-create/templates/CLAUDE.md.template
    </automated>
  </verify>

  <acceptance_criteria>
    - `test -f skills/workspace-create/templates/CLAUDE.md.template` exits 0 ("EXISTS")
    - `grep -c '{{WORKSPACE_NAME}}'` returns 1
    - `grep -c '{{WORKSPACE_GOAL}}'` returns 1
    - `grep -c '{{REPO_MAP}}'` returns 1
    - `grep -c '{{STACK}}'` returns 1
    - `grep -c '{{CREATED_DATE}}'` returns 1
    - `grep -c '{{CONVENTIONS}}'` returns 1
    - `wc -l` returns a number <= 60
    - `.gitkeep` no longer exists in `skills/workspace-create/templates/`
  </acceptance_criteria>

  <done>Template file exists at skills/workspace-create/templates/CLAUDE.md.template with all 6 markers present, Directory Map section hardcoded with 7 subdirectory rows, total ≤60 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Write workspace-create SKILL.md body and amend allowed-tools</name>

  <read_first>
    - skills/workspace-create/SKILL.md (current stub — frontmatter + placeholder comment to replace)
    - skills/workspace-create/templates/CLAUDE.md.template (just written in Task 1 — use exact marker names from this file)
    - skills/skill-create/SKILL.md (analog — mirror stage skeleton, 4-backtick fence pattern, validate→mkdir→Write order, final checks list structure)
    - .planning/phases/04-workspace-creator-skill/04-PATTERNS.md (stage skeleton with Phase 4 deviations, 10-item final checks adapted for workspace-create)
    - .planning/phases/04-workspace-creator-skill/04-RESEARCH.md (security patterns: name validation regex, path traversal rejection, overwrite warning, pitfalls 1-6)
  </read_first>

  <files>skills/workspace-create/SKILL.md</files>

  <action>
Replace the entire content of `skills/workspace-create/SKILL.md`. Keep the YAML frontmatter block but amend `allowed-tools` per Deviation D-01: change `[Write, Bash]` to `[Read, Write, Bash]`. Do NOT change any other frontmatter field.

Write the instruction body as a 9-stage linear sequence with a worked example and final checks section. Each stage is a `## Stage N: ...` heading. Follow the analog (skills/skill-create/SKILL.md) for heading style and prose density.

---

**Frontmatter (amended):**
```yaml
---
name: workspace-create
description: "Guides a workspace setup interview and scaffolds .workspace/, .claude/, .vscode/ directories with a fully populated CLAUDE.md. Use when the user invokes /workspace-create or asks to 'set up a workspace', 'create a workspace', or 'scaffold a new workspace'. Do NOT use for setting up individual project repos."
allowed-tools: [Read, Write, Bash]
disable-model-invocation: true
---
```

---

**Stage 1: Check for workspace name hint**

```
The user invoked this with: $ARGUMENTS

If $ARGUMENTS is non-empty and not only whitespace, treat it as the proposed workspace name.
Output: "I'll name this workspace `<$ARGUMENTS>` — change it?"
Then proceed to Stage 2 using the proposed name.

If $ARGUMENTS is empty or whitespace, proceed directly to Stage 2 with no proposed name.
```

---

**Stage 2: Conduct workspace interview**

Ask each question in chat (NOT via AskUserQuestion — these are freeform inputs that exceed AskUserQuestion's 4-option limit). Wait for the user's reply before asking the next question.

**Q1 — Workspace name** (skip if Stage 1 proposed a name and user confirmed it):
> "What should I name this workspace? Use lowercase letters, numbers, and hyphens only (e.g. `my-project`). This becomes the directory name."

Validate name immediately after receiving the answer: run in Bash:
```bash
NAME="<user-answer>"
if [[ ! "$NAME" =~ ^[a-z][a-z0-9-]*$ ]] || [[ "$NAME" == *".."* ]] || [[ "$NAME" == *"/"* ]] || [[ "$NAME" == *"\\"* ]]; then
  echo "INVALID"
else
  echo "VALID"
fi
```
If INVALID: explain the constraint and re-ask Q1. Do not proceed until validation passes.

**Q2 — Repos to include:**
> "Which repos will this workspace include? List them separated by commas or newlines."

**Q3 — Per-repo purpose (iterative):**
For each repo named in Q2, ask in chat:
> "What is the purpose of `<repo-name>`?"

Collect one answer per repo. If the user listed 3 repos, ask 3 separate questions.

**Q4 — Overall workspace goal:**
> "In 1-3 sentences, what is the overall goal of this workspace?"

If the answer is blank or less than 5 words, re-ask: "Could you describe the goal in a bit more detail? This becomes the opening paragraph of your CLAUDE.md."

**Q5 — Primary stack (optional):**
> "What is the primary language or stack? (Press Enter or type 'skip' to leave blank)"

If blank or 'skip', record as empty string — Stage 6 will use the fallback "Not specified".

---

**Stage 3: Show scaffold plan preview**

After all interview answers are collected, show the full scaffold plan in chat before writing anything. Use a 4-backtick outer fence for the preview block (prevents inner triple-backtick fences from terminating the preview):

````markdown
Workspace: <name>
Root: ./<name>/

Directory structure:
<name>/
├── CLAUDE.md
├── .workspace/
│   ├── refs/          README: Reference materials and external resources for this workspace.
│   ├── docs/          README: Documentation files and guides for workspace projects.
│   ├── logs/          README: Session logs and activity records.
│   ├── scratch/       README: Temporary scratch space for experiments and drafts.
│   ├── context/       README: Persistent context files shared across sessions.
│   ├── outputs/       README: Generated outputs and artifacts from Claude sessions.
│   └── sessions/      README: Per-session working directories.
├── .claude/
└── .vscode/

CLAUDE.md preview:
# <name>
<first 1-2 sentences of workspace goal>
**Created:** <date>
...
````

Then ask in chat (not via AskUserQuestion):
> "Shall I create all these files? Reply 'yes' to proceed or describe changes."

Wait for the user to reply before proceeding to Stage 4. If the user describes changes, incorporate them and show the updated preview. Do not proceed until the user says 'yes' or equivalent.

---

**Stage 4: Check for existing workspace**

Before any mkdir or Write:
```bash
WORKSPACE_ROOT="$(pwd)/<validated-name>"
test -d "$WORKSPACE_ROOT" && echo "EXISTS" || echo "NEW"
```

If the result is EXISTS, output the following warning in chat:
> "A directory named `<name>` already exists at `<WORKSPACE_ROOT>`. Proceeding will add files into it without deleting what's there. Continue?"

Wait for the user to confirm before proceeding. If they decline, stop.

---

**Stage 5: Scaffold directories**

Run a single Bash block creating all 9 directories atomically:
```bash
WORKSPACE_ROOT="$(pwd)/<validated-name>"
mkdir -p \
  "$WORKSPACE_ROOT/.workspace/refs" \
  "$WORKSPACE_ROOT/.workspace/docs" \
  "$WORKSPACE_ROOT/.workspace/logs" \
  "$WORKSPACE_ROOT/.workspace/scratch" \
  "$WORKSPACE_ROOT/.workspace/context" \
  "$WORKSPACE_ROOT/.workspace/outputs" \
  "$WORKSPACE_ROOT/.workspace/sessions" \
  "$WORKSPACE_ROOT/.claude" \
  "$WORKSPACE_ROOT/.vscode"
```

Do NOT use relative paths. Always use `$WORKSPACE_ROOT` (absolute via `$(pwd)`). Never use `$USERPROFILE` — this workspace is CWD-relative, not global.

---

**Stage 6: Write README files**

Write exactly 7 README.md files — one per .workspace/ subdirectory. Use Write tool for each. The content is a single line (no heading, no trailing newline issues):

- `$WORKSPACE_ROOT/.workspace/refs/README.md` → `Reference materials and external resources for this workspace.`
- `$WORKSPACE_ROOT/.workspace/docs/README.md` → `Documentation files and guides for workspace projects.`
- `$WORKSPACE_ROOT/.workspace/logs/README.md` → `Session logs and activity records.`
- `$WORKSPACE_ROOT/.workspace/scratch/README.md` → `Temporary scratch space for experiments and drafts.`
- `$WORKSPACE_ROOT/.workspace/context/README.md` → `Persistent context files shared across sessions.`
- `$WORKSPACE_ROOT/.workspace/outputs/README.md` → `Generated outputs and artifacts from Claude sessions.`
- `$WORKSPACE_ROOT/.workspace/sessions/README.md` → `Per-session working directories.`

---

**Stage 7: Generate CLAUDE.md content**

Read the template file:
```
${CLAUDE_SKILL_DIR}/templates/CLAUDE.md.template
```

This is a mandatory Read step — not implied behavior. The template contains 6 markers. Replace each marker with the interview answer:

| Marker | Source | Rule |
|--------|--------|------|
| `{{WORKSPACE_NAME}}` | Q1 answer | Required — re-ask if blank |
| `{{WORKSPACE_GOAL}}` | Q4 answer | Required — re-ask if blank |
| `{{REPO_MAP}}` | Q2+Q3 answers | Format each as a table row: `\| <repo> \| <purpose> \|` — one row per repo. Fallback if no repos: `No repos specified` |
| `{{STACK}}` | Q5 answer | Fallback: `Not specified` |
| `{{CREATED_DATE}}` | Bash `date +%Y-%m-%d` | Fallback: use today's date as literal string if Bash fails |
| `{{CONVENTIONS}}` | Derived: combine stack + goal into 1-2 practical rules | Fallback: `Follow standard conventions for the stack` |

After replacing all markers, verify the result line count:
```bash
echo "<generated-content>" | wc -l
```
If the count is >= 195, trim the `{{REPO_MAP}}` rows first (summarize multiple purposes on one row), then shorten the `{{CONVENTIONS}}` section, until the count is < 195. Only then write.

Write the populated content to `$WORKSPACE_ROOT/CLAUDE.md`.

**Zero tolerance for unreplaced markers:** Before writing, scan the generated content for any remaining `{{` patterns. If any are found, replace them with the appropriate fallback. Never write `{{MARKER}}` to the final CLAUDE.md.

---

**Stage 8: Write .claude/settings.local.json**

Write a minimal settings file inside the workspace's .claude directory:
```
$WORKSPACE_ROOT/.claude/settings.local.json
```
Content:
```json
{
  "permissions": {
    "allow": [
      "Write(.claude/**)"
    ]
  }
}
```

This mirrors the Phase 1 template and is a useful default for the workspace. It is workspace-local — it does NOT affect `~/.claude/`.

---

**Stage 9: Confirm**

Output in chat:
> "Workspace `<name>` created at `./<name>/`. CLAUDE.md populated. Open it to review."

---

**Worked example — end-to-end workspace creation**

When the user runs `/workspace-create my-apis`, the full flow looks like this:

- **Stage 1:** `$ARGUMENTS` = "my-apis" — propose name "my-apis", user confirms
- **Stage 2:** Q2 → "api-gateway, billing-service"; Q3 → "api-gateway: Routes all traffic", "billing-service: Handles subscriptions"; Q4 → "Track and debug API + billing interactions across services."; Q5 → "TypeScript, Node.js"
- **Stage 3:** Show preview tree with CLAUDE.md header; user replies "yes"
- **Stage 4:** `test -d "$(pwd)/my-apis"` → NEW; proceed
- **Stage 5:** Run single mkdir batch creating all 9 directories
- **Stage 6:** Write 7 README files with one-line content
- **Stage 7:** Read `${CLAUDE_SKILL_DIR}/templates/CLAUDE.md.template`; replace markers:
  - `{{WORKSPACE_NAME}}` → `my-apis`
  - `{{WORKSPACE_GOAL}}` → `Track and debug API + billing interactions across services.`
  - `{{REPO_MAP}}` → `| api-gateway | Routes all traffic |\n| billing-service | Handles subscriptions |`
  - `{{STACK}}` → `TypeScript, Node.js`
  - `{{CREATED_DATE}}` → `2026-04-29` (from Bash)
  - `{{CONVENTIONS}}` → `Use async/await consistently. Avoid mutating shared state across services.`
  - Verify: `wc -l` → 58 lines (< 195 — no trimming needed)
  - Verify: no `{{` patterns remain
  - Write to `my-apis/CLAUDE.md`
- **Stage 8:** Write `my-apis/.claude/settings.local.json`
- **Stage 9:** "Workspace `my-apis` created at `./my-apis/`. CLAUDE.md populated. Open it to review."

---

**Final checks before writing**

Before executing Stage 5 (mkdir), confirm:
1. Workspace name validated with `^[a-z][a-z0-9-]*$` — no `/`, `..`, or `\` in name.
2. All 5 interview questions answered (name, repos, per-repo purpose, goal, stack — or blank/skip recorded for Q5).
3. `{{WORKSPACE_NAME}}` and `{{WORKSPACE_GOAL}}` are non-empty (re-asked if needed).
4. `{{STACK}}` has either a user-provided value or the fallback "Not specified".
5. `{{REPO_MAP}}` has either formatted table rows or the fallback "No repos specified".
6. `{{CONVENTIONS}}` has either a derived value or the fallback "Follow standard conventions for the stack".
7. Scaffold plan preview shown to user; user replied 'yes' or equivalent.
8. Existing workspace check ran before mkdir; user confirmed if EXISTS.
9. All 9 `mkdir -p` paths use `$WORKSPACE_ROOT` (absolute) — no relative paths, no `$USERPROFILE`.
10. All 6 markers replaced in generated CLAUDE.md; zero `{{` patterns remain.
11. `wc -l` on generated CLAUDE.md content is < 195 before Write.
12. All Write paths use `$WORKSPACE_ROOT` — not `~`, not `$USERPROFILE`, not a hardcoded path.
  </action>

  <verify>
    <automated>
      # Frontmatter amended (D-01)
      grep -c 'allowed-tools: \[Read, Write, Bash\]' skills/workspace-create/SKILL.md

      # WORK-01: Skill body present
      grep -c 'workspace' skills/workspace-create/SKILL.md

      # WORK-02: Interview covers all required inputs
      grep -c 'repo' skills/workspace-create/SKILL.md

      # WORK-03: All 7 .workspace/ subdirs in mkdir instruction
      grep -c '\.workspace' skills/workspace-create/SKILL.md

      # WORK-04: .claude/ and .vscode/ in mkdir instruction
      grep -c '\.claude' skills/workspace-create/SKILL.md
      grep -c '\.vscode' skills/workspace-create/SKILL.md

      # WORK-05: Template reference present
      grep -c 'CLAUDE_SKILL_DIR.*templates.*CLAUDE' skills/workspace-create/SKILL.md

      # WORK-05: 200-line guard instruction present
      grep -c '200\|195\|wc -l' skills/workspace-create/SKILL.md

      # WORK-06: README write instruction for each subdir
      grep -c 'README' skills/workspace-create/SKILL.md

      # Security: path traversal validation present
      grep -c 'a-z\]\[a-z0-9-\]' skills/workspace-create/SKILL.md

      # Security: $(pwd) used (not $USERPROFILE) for workspace root
      grep -c '\$(pwd)' skills/workspace-create/SKILL.md

      # Security: $USERPROFILE must not appear as a path prefix in scaffold paths (Phase 4 uses CWD-relative paths)
      # Trailing slash makes this a path-context match; prose like 'not $USERPROFILE' has no trailing slash and won't match
      grep -c '\$USERPROFILE/' skills/workspace-create/SKILL.md || echo 0
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'allowed-tools: \[Read, Write, Bash\]'` returns 1 (frontmatter amended per D-01)
    - `grep -c 'workspace'` returns >= 10 (body is substantive)
    - `grep -c 'repo'` returns >= 5 (interview covers repos)
    - `grep -c '\.workspace'` returns >= 9 (all 7 subdirs + .workspace container references)
    - `grep -c '\.claude'` returns >= 2 (mkdir + settings.local.json write stage)
    - `grep -c '\.vscode'` returns >= 1 (mkdir stage)
    - `grep -c 'CLAUDE_SKILL_DIR.*templates.*CLAUDE'` returns >= 1 (template reference present)
    - `grep -c '195\|wc -l'` returns >= 1 (line guard instruction present)
    - `grep -c 'README'` returns >= 7 (one instruction per subdir)
    - `grep -c '\$(pwd)'` returns >= 2 (workspace root resolved via pwd, not hardcoded)
    - `grep -c '\$USERPROFILE/' skills/workspace-create/SKILL.md` returns 0 — no $USERPROFILE path prefix in scaffold stages (prose negations like 'not $USERPROFILE' lack a trailing slash and are excluded)
    - `wc -l skills/workspace-create/SKILL.md` returns >= 150 (body is complete, not a stub)
  </acceptance_criteria>

  <done>skills/workspace-create/SKILL.md contains a complete 9-stage instruction body with worked example and final checks. Frontmatter has `allowed-tools: [Read, Write, Bash]`. All 6 markers from CLAUDE.md.template are referenced by exact name. Validation regex is `^[a-z][a-z0-9-]*$`. No $USERPROFILE in scaffold paths. All WORK-01 through WORK-06 requirements structurally addressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-input → workspace name | User-supplied name becomes a directory path component — must be validated before any mkdir or Write |
| CWD → WORKSPACE_ROOT | WORKSPACE_ROOT is derived from $(pwd); must be a subdirectory of CWD, not root or home |
| template markers → CLAUDE.md output | All {{MARKERS}} must be replaced before Write; unreplaced markers are an information disclosure risk |
| workspace .claude/ → global ~/.claude/ | Local workspace settings must never write to or be confused with the global ~/.claude/ path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Workspace name input (Stage 2 Q1) | mitigate | Stage 2 Q1 validates `^[a-z][a-z0-9-]*$` in Bash before any mkdir/Write call. Name must not contain `/`, `..`, or `\`. Validation failure stops the flow — explicit error message, Q1 re-asked. This gates all downstream file operations. |
| T-04-02 | Tampering | Existing workspace directory (Stage 4) | mitigate | Stage 4 runs `test -d "$WORKSPACE_ROOT"` before any mkdir; if EXISTS, warns user and requires explicit confirmation before proceeding. If user declines, flow stops. Prevents silent overwrite of existing workspaces. |
| T-04-03 | Information Disclosure | Generated CLAUDE.md output (Stage 7) | mitigate | Stage 7 final-checks step scans generated content for any remaining `{{` patterns before Write. Required markers (`{{WORKSPACE_NAME}}`, `{{WORKSPACE_GOAL}}`) are re-asked if blank. Optional markers (`{{STACK}}`, `{{CONVENTIONS}}`) receive fallback strings. Template stub content `{{MARKER}}` is never written to the final file. |
| T-04-04 | Elevation of Privilege | WORKSPACE_ROOT path resolution (Stage 5) | mitigate | WORKSPACE_ROOT is resolved once as `$(pwd)/<validated-name>` — a subdirectory of the user's current working directory. Never uses `$USERPROFILE`, `~`, or absolute paths outside CWD. All mkdir and Write calls use `$WORKSPACE_ROOT` (the validated absolute path), preventing scaffold to unexpected directories such as system root or home. |
</threat_model>

<verification>
Run after both tasks complete:

```bash
# Template file exists with all 6 markers
test -f skills/workspace-create/templates/CLAUDE.md.template && echo "TEMPLATE: EXISTS" || echo "TEMPLATE: MISSING"
for marker in WORKSPACE_NAME WORKSPACE_GOAL REPO_MAP STACK CREATED_DATE CONVENTIONS; do
  count=$(grep -c "{{$marker}}" skills/workspace-create/templates/CLAUDE.md.template 2>/dev/null)
  echo "MARKER {{$marker}}: $count (expect 1)"
done

# SKILL.md body is complete
wc -l skills/workspace-create/SKILL.md
grep -c 'allowed-tools: \[Read, Write, Bash\]' skills/workspace-create/SKILL.md

# All 6 markers referenced in SKILL.md
for marker in WORKSPACE_NAME WORKSPACE_GOAL REPO_MAP STACK CREATED_DATE CONVENTIONS; do
  count=$(grep -c "{{$marker}}" skills/workspace-create/SKILL.md 2>/dev/null)
  echo "SKILL ref {{$marker}}: $count (expect >= 1)"
done

# Security checks
grep -c '\$(pwd)' skills/workspace-create/SKILL.md
grep -c '^\[a-z\]\[a-z0-9-\]' skills/workspace-create/SKILL.md || grep -c 'a-z\]\[a-z0-9' skills/workspace-create/SKILL.md
```
</verification>

<success_criteria>
Phase 4 is complete when:
1. `skills/workspace-create/templates/CLAUDE.md.template` exists with all 6 markers (`{{WORKSPACE_NAME}}`, `{{WORKSPACE_GOAL}}`, `{{REPO_MAP}}`, `{{STACK}}`, `{{CREATED_DATE}}`, `{{CONVENTIONS}}`) and total line count <= 60
2. `skills/workspace-create/SKILL.md` has `allowed-tools: [Read, Write, Bash]` in frontmatter
3. `wc -l skills/workspace-create/SKILL.md` >= 150 (body is substantive, not a stub)
4. SKILL.md references `${CLAUDE_SKILL_DIR}/templates/CLAUDE.md.template` by exact path
5. SKILL.md uses validation regex `^[a-z][a-z0-9-]*$` for workspace name
6. SKILL.md uses `$(pwd)/<validated-name>` for WORKSPACE_ROOT (not `$USERPROFILE`, not `~`)
7. SKILL.md contains 200-line guard instruction for generated CLAUDE.md
8. SKILL.md contains 12-item final checks list before the write stage
9. Manual UAT: `/workspace-create` in a live session creates `.workspace/` tree, populates CLAUDE.md with no `{{` markers, all 7 subdirectory READMEs present
</success_criteria>

<output>
After completion, create `.planning/phases/04-workspace-creator-skill/04-01-SUMMARY.md` using the summary template.
</output>
